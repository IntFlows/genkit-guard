We welcome contributions.

## Areas to Contribute

* New guard types

* Additional PII formats

* Performance improvements

* Documentation
## v0.0.14 validation

Run `npm install`, `npm test` and `npm run build`. Configuration tests stub model pipelines and verify startup/runtime agreement and label mappings. Real Redis checks run separately with `npm run test:redis`. Preserve inline options, no-argument initialization and independent flow configurations.
